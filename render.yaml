services:
  - type: web
    name: flashread
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    autoDeployTrigger: commit
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: MONGODB_DB
        value: flashread
      - key: JWT_SECRET
        generateValue: true
